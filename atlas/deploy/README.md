# Deploy — running AtlasOS for real

Running AtlasOS on real infrastructure: one chosen cloud (Google Cloud, AWS, or Azure),
routines that fire on a schedule or webhook, and sandboxes for safe execution.

**Built in:** Module 7. Pick **one** provider as your primary (L24 GCP, L25 AWS, or L26
Azure/Foundry) rather than learning all three.

## TODO (fill in as you go)
- [ ] M7: choose the primary cloud and document why
- [ ] M7: deploy the orchestrator + first agent so it survives a closed laptop
- [ ] M7: schedule a routine; confirm it runs unattended
