# Tools / MCP layer

How agents reach the outside world: web, databases, internal APIs, the file system, and your
own MCP servers. Keep tool schemas lean (tool search, programmatic tool calling) so the
context window stays cheap.

**Introduced just-in-time;** matters most from Module 5 onward. See
[modelcontextprotocol.io](https://modelcontextprotocol.io/) when you reach it.

## TODO (fill in as you go)
- [ ] M4 L10: harness hooks and context-window economics
- [ ] M5: the tools each agent needs (web/search for Scout, etc.)
- [ ] M5+: connect at least one MCP server
