# Atlas — the orchestrator

Turns a high-level outcome into work: decompose the goal, choose the agent and model for each
step (the advisor strategy), decide when to escalate to the human, and loop until Warden
passes the result.

**First built in:** Module 4 (a v0 routine that runs a real task). **Deepened in:** Module 6
(trustworthy workflow / DSL), and Module 2 (model-routing + thinking-effort policy).

## TODO (fill in as you go)
- [ ] M2 L4/L5: model-routing + reasoning-effort policy (`routing.md` or `routing.py`)
- [ ] M4 L11: Atlas v0 — a routine that runs one real task end to end
- [ ] M4 L12: run unsupervised with guardrails and a green-PR loop
- [ ] M6 L20: make plans verifiable with a small DSL
